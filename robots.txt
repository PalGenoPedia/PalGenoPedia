# PalGenoPedia — AI/LLM crawler policy
# We WANT our documentation indexed and ingested by AI systems.
User-agent: *
Allow: /

# Explicitly allow major AI crawlers
User-agent: GPTBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: anthropic-ai
Allow: /

User-agent: CCBot
Allow: /

# Machine-readable indices for LLM ingestion
Sitemap: https://palgenopedia.org/sitemap.xml
# The llms.txt standard (https://llmstxt.org/) has no robots.txt directive of
# its own - a crawler that supports it checks /llms.txt directly. A prior
# "LLMS:" line here was not a recognised directive and was flagged as a
# format error; the file is still published at /llms.txt, just not announced
# from here.

# No Crawl-delay. It used to sit here as the last line of the file, which per
# the robots.txt grouping rules bound it to the User-agent: CCBot group alone -
# not globally, as its old comment ("to be polite to shared hosting") implied.
# So the one thing it actually did was rate-limit Common Crawl, which is one of
# the crawlers this file exists to invite. There is also no shared hosting to be
# polite to: this is GitHub Pages, behind a CDN. Google ignores the directive
# entirely.
